# Horizon Demo WebComponent

Dummy native WebComponent application for validating the Horizon DevOps Pipeline service.

Use this repo with:

- `ProjectType`: `WebComponent`
- `RepositoryType`: `GitHub`
- `Branch`: `main`

Build commands expected by the pipeline:

- `npm install`
- `npm run build`
- Docker image build from `Dockerfile`
